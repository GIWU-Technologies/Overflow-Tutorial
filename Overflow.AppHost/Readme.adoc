# README

## Overview

* Keycloak login info
    ** URL: http://localhost:6001/auth
    ** Realm: master
    ** Username: kc-admin
    ** Password: Pa$$w0rd

* User secrets
    ** dotnet user-secrets set "Parameters:typesense-api-key" "abc"

* To deploy to docker (locally)
    ** aspire deploy -o infra

* To shut docker compose service down
    ** aspire do docker-compose-down-production -o infra

* To export keycloak realm configuration (from infra folder)
    ** docker run --rm \
-v keycloak-data:/opt/keycloak/data \
-v $(pwd)/realms:/opt/keycloak/export \
quay.io/keycloak/keycloak:26.5 \
export --realm overflow --dir /opt/keycloak/export --users realm_file


    
